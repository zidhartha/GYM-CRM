spring:
  activemq:
    broker-url: vm://localhost?broker.persistent=false&broker.useJmx=false
    user: admin
    password: admin

eureka:
  client:
    enabled: false

activemq:

  queue:
    workload: gym.workload.queue
    dlq: DLQ.gym.workload.queue


de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.11