server:
  port: 8761

eureka:
  client:
    # This does not register itself and does not fetch its own registry.
    register-with-eureka: false
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0

spring:
  application:
    name: eureka-server
